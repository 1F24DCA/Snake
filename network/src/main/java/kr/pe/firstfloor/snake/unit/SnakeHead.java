package kr.pe.firstfloor.snake.unit;

public class SnakeHead extends Snake {
    protected SnakeHead() {}
    protected SnakeHead(String userId, int x, int y, int direction) {
        super(userId, x, y, direction);
    }
}
