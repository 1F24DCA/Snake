apply plugin: 'java-library'

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
    try { annotationProcessor project(":processor") } catch (Exception e) { e.printStackTrace() }
}

sourceCompatibility = "8"
targetCompatibility = "8"
